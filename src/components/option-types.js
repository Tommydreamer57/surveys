import React from 'react';

const Radio = question => (
    <input />
)

export default Radio

export const Checkbox = question => (
    <input />
)

export const Option = question => (
    <option />
)
